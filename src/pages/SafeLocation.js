import React, { Component } from 'react';
import RestList from './../components/RestList';

class SafeLocation extends Component {
  render() {
    return (
      <div>
        <RestList />
      </div>
    );
  }
}

export default SafeLocation;